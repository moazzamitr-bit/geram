# Environment — Gram

# Never commit real secrets.

NEXT_PUBLIC_APP_MODE=live
NEXT_PUBLIC_DEMO_OTP=123456

# Supabase project (Owj / geram)
NEXT_PUBLIC_SUPABASE_URL=https://wlxlobhetjbrhbfpsmzp.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
PHONE_AUTH_PEPPER=
# Optional privileged key (admin bulk ops / price persist / cron DCA)
SUPABASE_SERVICE_ROLE_KEY=
# Bearer secret for /api/cron/revenue (Vercel Cron)
CRON_SECRET=

# Live gold price: GET /api/market/price
# Optional: ?persist=1 writes into market_prices (needs SERVICE_ROLE).
