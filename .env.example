# Environment — Gram

# Never commit real secrets.

NEXT_PUBLIC_APP_MODE=sandbox
NEXT_PUBLIC_DEMO_OTP=123456

# Supabase project: https://supabase.com/dashboard/project/ogirzyxcamuxrsenpdal
NEXT_PUBLIC_SUPABASE_URL=https://ogirzyxcamuxrsenpdal.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Live gold price is fetched server-side from TGJU public feed
# via GET /api/market/price (polled every 30s by the app).
# Optional: ?persist=1 writes the quote into market_prices (needs SERVICE_ROLE).
